module.exports = {
    content: ['./**/*.{js,jsx,ts,tsx}'],
    theme: {
        extend: {
            colors: {
                // 커스텀 색상이 필요한 경우 여기에 추가
            }
        }
    },
    plugins: []
}
